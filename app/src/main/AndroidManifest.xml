<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Maid Hub"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NoPrev"></activity>
        <activity android:name=".NoDataFound" />
        <activity android:name=".CollectAmt" />
        <activity android:name=".ProviderHome" />
        <activity android:name=".PreviousRequests" />
        <activity android:name=".ViewRequest" />
        <activity android:name=".NewRequest" />
        <activity android:name=".HouseHome" />
        <activity android:name=".Registration" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>